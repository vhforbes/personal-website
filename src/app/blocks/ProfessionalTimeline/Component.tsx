type Props = {
  name: string
}

export const ProfessionalTimelineBlock: React.FC<Props> = (props) => {
  const { name } = props

  return <div className="mt-10 bg-primary">Hey I`m Professional Timeline</div>
}
