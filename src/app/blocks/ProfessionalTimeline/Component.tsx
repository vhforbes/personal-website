type Props = {
  name: string
}

export const ProfessionalTimelineBlock: React.FC<Props> = (props) => {
  const { name } = props

  return <div className="mt-10 bg-amber-600 text-accent">Hey I`m Professional Timeline</div>
}
